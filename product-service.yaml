server:
  port: 8083

spring:
  application:
    name: product-service

resilience4j:
  circuitbreaker:
    instances:
      productCB:
        slidingWindowSize: 5
        minimumNumberOfCalls: 3
        failureRateThreshold: 50
